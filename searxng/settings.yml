use_default_settings: true

general:
  instance_name: "boe-sanciones-searxng"

search:
  formats:
    - html
    - json

server:
  # Local-only instance on the internal Docker network, not exposed publicly.
  # A real secret would be needed if this were ever reachable from outside.
  secret_key: "local-dev-only-not-a-real-secret"
  limiter: false
  image_proxy: false
